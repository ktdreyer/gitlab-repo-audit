# gitlab-repo-audit

Read `docs/spec.md` before making changes. Update the spec first, then implement against it.

Run tests with `hatch run test` or `.venv/bin/pytest tests/`.

Run the linter with `hatch run lint`.
